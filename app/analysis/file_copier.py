# Located in app/analysis/file_copier.py
def copy_files_to_data(root_directory, data_directory, include_extensions):
    # Dummy file copying: Print a message that files would be copied
    print(f"Files would be copied from {root_directory} to {data_directory}")
